name: Publicar no GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run build
        env:
          BASE_PATH: /${{ github.event.repository.name }}/
          # chaves públicas do Supabase (podem ficar no código; os dados são protegidos pelo login + RLS)
          VITE_SUPABASE_URL: https://wijjvkjtdlaxyluczufe.supabase.co
          VITE_SUPABASE_ANON_KEY: sb_publishable_K3EoiOt32cm2ereZj1jOQg_HbV2HZ_Q
      # rotas como /vendas funcionam ao recarregar a página
      - run: cp dist/index.html dist/404.html
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
